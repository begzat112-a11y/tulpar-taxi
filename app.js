let tariffPrice=350;let orders=JSON.parse(localStorage.getItem('tulpar_orders')||'[]');let driverOnline=false;
function showTab(id){document.querySelectorAll('.panel').forEach(x=>x.classList.remove('active'));document.getElementById(id).classList.add('active');document.querySelectorAll('.tab').forEach(x=>x.classList.toggle('active',x.dataset.tab===id));if(id==='admin')renderAdmin()}
function selectTariff(el,p){document.querySelectorAll('.tariff').forEach(x=>x.classList.remove('selected'));el.classList.add('selected');tariffPrice=p;document.getElementById('price').textContent=p+' сом'}
function orderTaxi(){const from=document.getElementById('from').value.trim(),to=document.getElementById('to').value.trim();if(!from||!to)return;const order={id:Date.now(),from,to,price:tariffPrice,status:'Ищем машину'};orders.unshift(order);save();document.getElementById('orderStatus').textContent='Заказ №'+order.id+' создан. Ищем водителя…';setTimeout(()=>{order.status='Водитель найден';save();document.getElementById('orderStatus').textContent='🚗 Водитель найден. Это демонстрационный режим.';renderAdmin()},1200)}
function toggleDriver(){driverOnline=document.getElementById('online').checked;document.getElementById('driverStatus').textContent='Статус: '+(driverOnline?'онлайн 🟢':'офлайн');document.getElementById('drivers').textContent=driverOnline?'1':'0'}
function acceptOrder(){let o=orders.find(x=>x.status==='Водитель найден'||x.status==='Ищем машину');if(!o){document.getElementById('driverStatus').textContent='Новых заказов нет';return}o.status='Водитель едет к клиенту';save();document.getElementById('driverStatus').textContent='Заказ №'+o.id+' принят 🚕';renderAdmin()}
function save(){localStorage.setItem('tulpar_orders',JSON.stringify(orders));renderAdmin()}
function renderAdmin(){document.getElementById('orders').textContent=orders.length;const box=document.getElementById('adminList');if(!orders.length){box.innerHTML='<p>Пока нет заказов.</p>';return}box.innerHTML=orders.slice(0,10).map(o=>`<div class="order"><b>№${o.id}</b><span>${o.from} → ${o.to}</span><small>${o.price} сом • ${o.status}</small></div>`).join('')}
renderAdmin();